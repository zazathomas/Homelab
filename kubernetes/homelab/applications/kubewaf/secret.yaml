---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: envoy-kubewaf
spec:
  refreshInterval: 24h
  secretStoreRef:
    kind: ClusterSecretStore
    name: oci-vault-secretstore # Must match SecretStore on the cluster
  target:
    name: envoy-kubewaf # Name for the secret on the cluster
    creationPolicy: Owner
  dataFrom:
    - extract: 
        conversionStrategy: Default	
        decodingStrategy: None
        key: envoy-kubewaf # Name in the OCI Vault
        metadataPolicy: None